import { Collection, Snowflake, ThreadMember, ThreadChannel } from "discord.js";
import WOK from "wokcommands";

export default (addMembers: Collection<Snowflake,ThreadMember>, removedMembers: Collection<Snowflake,ThreadMember>, thread: ThreadChannel, instance: WOK) => {
  
};