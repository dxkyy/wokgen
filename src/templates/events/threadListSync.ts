import { Collection, Snowflake, ThreadChannel, Guild } from "discord.js";
import WOK from "wokcommands";

export default (threads: Collection<Snowflake,ThreadChannel>, guild: Guild, instance: WOK) => {
  
};