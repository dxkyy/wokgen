import { Collection, Snowflake,GuildMember, Guild } from "discord.js";
import WOK from "wokcommands";

export default (members: Collection<Snowflake,GuildMember>, guild: Guild, instance: WOK) => {
  
};